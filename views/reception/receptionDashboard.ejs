<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard | MedSys+</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/admin.css" />
    <style>
        /* Navbar height is usually 56px in Bootstrap */
        body {
            padding-top: 56px;
            /* Push all content below navbar */
        }

        .sidebar {
            height: calc(100vh - 56px);
            margin-top: 56px;
            position: fixed;
            top: 0;
            left: 0;
            width: 16.66%;
            background-color: #f8f9fa;
            overflow-y: auto;
            border-right: 1px solid #dee2e6;
        }

        /* Main content area */
        .main-content {
            margin-left: 16.66%;
            padding: 20px;
        }
    </style>
</head>

<body>

    <!-- Include navbar (if any) -->
    <%- include('partials/header') %>
        <div class="container-fluid">
            <div class="row">

                <!-- Sidebar -->
                <nav class="col-md-2 d-none d-md-block bg-light sidebar py-4">
                    <div class="position-sticky">
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link %>" href="/admin/dashboard?page=add-doctor">
                                    Add Doctor
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link " href="#">
                                    Add Reception
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link " href="#">
                                    View Patients
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-danger" href="/auth/logout">
                                    Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                </nav>

                <!-- Main Content (dynamic) -->
                <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 pt-4 main-content">
                    <h2 class="justify-content-center">
                        Welcome, Receptionist
                    </h2>

                    <% if (partialToInclude) { %>
                        <%- include(main_Content) %>
                            <% }%>


                </main>
            </div>
        </div>
</body>

</html>